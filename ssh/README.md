## :key: SSH en NetBSD 10.1 con llave pública
Es importante ingresar al servidor remotamente de manera segura, por defecto **ssh** viene en la base del sistema, por tanto
solamente debemos activar el servicio cuando se configura detalles en el proceso de la instalación.
